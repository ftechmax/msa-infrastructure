---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rabbitmq-management
spec:
  parentRefs:
    - name: gateway
      namespace: istio-ingress
  hostnames:
    - "rabbitmq.kube.local"
  rules:
    - backendRefs:
        - name: rabbitmq
          port: 15672
